# 数据库配置
database:
  driver: mysql
  host: localhost
  port: 3306
  username: root
  password: wangjunyi
  dbname: smart_school
  charset: utf8mb4
  parseTime: true
  loc: Local

# 服务器配置
server:
  port: 8080
  mode: debug  # debug, release, test

# JWT配置
jwt:
  secret: smart_school_secret_key
  expire: 24h  # 过期时间

# AI对话配置
ai:
  coze:
    url: "https://api.coze.cn/v1/workflow/stream_run"
    token: "pat_gaF9SalaATC196OLGavZQBicUVHRlqvg4TPM5m3jmF3PiiKxpcKlBZP4p004e8K3"
    workflow_id: "7559133188720017417"

# RabbitMQ 配置
rabbitmq:
  url: "amqp://guest:guest@localhost:5672/"

# --- 新增：Redis 配置 ---
redis:
  addr: "localhost:6379" # Redis 的地址和端口
  password: ""            # Redis 的密码，我们本地没有设置，所以为空
  db: 0                   # 使用的数据库编号，0 是默认值